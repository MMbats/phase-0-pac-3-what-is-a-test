// index.js
const name = "Susan"; // Set name to "Susan" to match the test requirement
const height = 30;    // Set height to a value less than 40
const message = `${name} is ${height} inches tall`; // Create a message using string interpolation

module.exports = { name, height, message };
